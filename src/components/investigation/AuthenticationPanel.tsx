import type { AnalysisResult, AuthenticationResult } from '../../types/analysis'

const explanations: Record<keyof AuthenticationResult, string> = { spf: 'Sender IP policy evaluation', dkim: 'Message signature validation', dmarc: 'Domain alignment policy evaluation' }
export function AuthenticationPanel({ result }: { result: AnalysisResult }) { return <section className="investigation-panel"><div className="investigation-section-heading"><div><p className="panel-kicker">Authentication</p><h2>Identity checks</h2></div></div><div className="auth-list">{(Object.entries(result.authentication) as [keyof AuthenticationResult, AuthenticationResult[keyof AuthenticationResult]][]).map(([name, value]) => <div className="auth-row" key={name}><strong>{name.toUpperCase()}</strong><span className={`auth-result auth-${value.toLowerCase()}`}>{value.toUpperCase()}</span><small>{explanations[name]}</small></div>)}</div><p className="panel-note">Authentication results are evidence signals and do not alone establish malicious intent.</p></section> }