import { Activity, AlertTriangle, ShieldCheck } from 'lucide-react'
import { SeverityBadge } from '../ui/SeverityBadge'
import type { AnalysisResult } from '../../types/analysis'

export function ThreatAssessment({ result }: { result: AnalysisResult }) { const summary = result.severity === 'Low' ? 'The message aligns with expected sender and content patterns, with no material threat signals observed.' : `This message contains indicators associated with ${result.threatType.toLowerCase()}, including authentication anomalies and sender or content inconsistencies.`; return <section className="investigation-panel assessment-panel"><div className="investigation-section-heading"><div><p className="panel-kicker">Threat assessment</p><h2>What happened?</h2></div><span className="assessment-status"><Activity size={14} />Analysis complete</span></div><div className="assessment-grid"><div className="assessment-score"><span>Risk Score</span><strong>{result.riskScore}<small>/ 100</small></strong><div className="score-track"><span style={{ width: `${result.riskScore}%` }} /></div></div><div className="assessment-item"><span>Threat type</span><strong>{result.threatType}</strong></div><div className="assessment-item"><span>Severity</span><SeverityBadge severity={result.severity} /></div><div className="assessment-item"><span>Analysis status</span><strong className="status-text"><ShieldCheck size={15} />Complete</strong></div></div><div className="assessment-summary"><AlertTriangle size={16} /><div><strong>Assessment summary</strong><p>{summary}</p></div></div></section> }