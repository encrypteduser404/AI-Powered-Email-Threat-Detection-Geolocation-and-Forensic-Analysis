import { Save } from 'lucide-react'
import { useState } from 'react'

export function AnalystNotes({ reviewed, onReviewed }: { reviewed: boolean; onReviewed: () => void }) { const [notes, setNotes] = useState(''); const [saved, setSaved] = useState(false); return <section className="investigation-panel notes-panel"><div className="investigation-section-heading"><div><p className="panel-kicker">Analyst record</p><h2>Analyst notes</h2></div><span className="section-count">Frontend only</span></div><div className="notes-grid"><label>Analyst<input value="Security Operations" readOnly /></label><label>Status<select value={reviewed ? 'Reviewed' : 'Open'} onChange={(event) => { if (event.target.value === 'Reviewed') onReviewed() }}><option>Open</option><option>Reviewed</option></select></label><label className="notes-field">Notes<textarea value={notes} onChange={(event) => { setNotes(event.target.value); setSaved(false) }} placeholder="Record observations for the investigation record..." /></label></div><div className="notes-actions"><span>{saved ? 'Note saved locally.' : 'Notes are not persisted yet.'}</span><button className="button button-secondary" onClick={() => setSaved(true)}><Save size={14} />Save note</button><button className="button button-primary" onClick={onReviewed}>{reviewed ? 'Reviewed' : 'Mark reviewed'}</button></div></section> }