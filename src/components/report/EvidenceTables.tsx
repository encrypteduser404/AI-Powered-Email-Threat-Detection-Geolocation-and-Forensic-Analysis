import type { ForensicReport } from '../../types/report'
import { ReportSection } from './ReportSection'

export function EvidenceTables({ report }: { report: ForensicReport }) { return <ReportSection eyebrow="Infrastructure" title="Observed Infrastructure"><div className="report-unavailable">Enrichment unavailable for {report.incident.incident_id}</div><p className="report-note">No geolocation or infrastructure enrichment is available in this milestone.</p></ReportSection> }

export function RoutingEvidence({ report }: { report: ForensicReport }) { return <ReportSection eyebrow="Routing evidence" title="Received Chain"><div className="report-table-wrap"><table className="report-table"><thead><tr><th>Hop</th><th>Observed value</th></tr></thead><tbody>{report.routing.received_hops.map((hop, index) => <tr key={`${index}-${hop}`}><td>{index + 1}</td><td className="mono wrap-cell">{hop}</td></tr>)}</tbody></table></div><p className="report-note">Values are observed routing evidence. IP candidates are not identified as attacker or source IPs.</p></ReportSection> }
export function UrlEvidenceReport({ report }: { report: ForensicReport }) { return <ReportSection eyebrow="Web evidence" title="URL Manifest"><div className="report-table-wrap"><table className="report-table"><thead><tr><th>ID</th><th>URL</th><th>Domain</th><th>Scheme</th><th>Indicators</th><th>Status</th><th>Risk</th></tr></thead><tbody>{report.url_evidence.map((url, index) => <tr key={url.url}><td>URL-{index + 1}</td><td className="mono wrap-cell">{url.url}</td><td className="mono">{url.domain}</td><td>{url.scheme}</td><td>{url.indicators.join(', ') || 'None'}</td><td>{url.status}</td><td>+{url.risk_contribution}</td></tr>)}</tbody></table></div></ReportSection> }
export function AttachmentEvidenceReport({ report }: { report: ForensicReport }) { return <ReportSection eyebrow="Artifact evidence" title="Attachment Manifest"><div className="report-table-wrap"><table className="report-table"><thead><tr><th>ID</th><th>Filename</th><th>Content type</th><th>Size</th><th>SHA-256</th><th>Status</th><th>Indicators</th></tr></thead><tbody>{report.attachment_evidence.map((attachment, index) => <tr key={attachment.sha256}><td>ATT-{index + 1}</td><td className="mono">{attachment.filename}</td><td>{attachment.content_type}</td><td>{attachment.size_bytes} bytes</td><td className="mono wrap-cell">{attachment.sha256}</td><td>{attachment.status}</td><td>{attachment.indicators.join(', ') || 'None'}</td></tr>)}</tbody></table></div></ReportSection> }
export function IocManifest({ report }: { report: ForensicReport }) { return <ReportSection eyebrow="Correlation" title="IOC Manifest"><div className="report-table-wrap"><table className="report-table"><thead><tr><th>IOC ID</th><th>Type</th><th>Value</th><th>Source</th><th>Status</th></tr></thead><tbody>{report.ioc_manifest.map((ioc, index) => <tr key={`${ioc.type}-${ioc.value}`}><td>IOC-{index + 1}</td><td>{ioc.type}</td><td className="mono wrap-cell">{ioc.value}</td><td>{ioc.source}</td><td>{ioc.status}</td></tr>)}</tbody></table></div></ReportSection> }