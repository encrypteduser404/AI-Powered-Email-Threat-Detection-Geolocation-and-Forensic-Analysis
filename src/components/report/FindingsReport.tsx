import type { ForensicReport } from '../../types/report'
import { ReportSection } from './ReportSection'
export function FindingsReport({ report }: { report: ForensicReport }) { return <ReportSection eyebrow="Analytical evidence" title="Findings"><div className="report-table-wrap"><table className="report-table"><thead><tr><th>ID</th><th>Category</th><th>Title</th><th>Severity</th><th>Evidence</th><th>Description</th><th>Risk</th></tr></thead><tbody>{report.findings.map((finding, index) => <tr key={`${finding.title}-${index}`}><td>{finding.id || `FND-${index + 1}`}</td><td>{finding.category || 'Analysis'}</td><td>{finding.title}</td><td>{finding.severity}</td><td className="evidence-cell">{finding.evidence || 'Unavailable'}</td><td>{finding.detail}</td><td>+{finding.riskContribution ?? 0}</td></tr>)}</tbody></table></div></ReportSection> }