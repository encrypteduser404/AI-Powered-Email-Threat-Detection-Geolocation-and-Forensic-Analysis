import type { ForensicReport } from '../../types/report'
import { ReportSection } from './ReportSection'
export function AuthenticationReport({ report }: { report: ForensicReport }) { return <ReportSection eyebrow="Identity evidence" title="Authentication"><div className="report-field-table">{Object.entries(report.authentication.results).map(([name, value]) => <div key={name}><span>{name.toUpperCase()}</span><strong className={`auth-result auth-${String(value).toLowerCase()}`}>{String(value)}</strong></div>)}</div><p className="report-note">Observed authentication results are reproduced from message headers. No cryptographic verification or live DNS policy evaluation was performed.</p><details className="report-details"><summary>Raw authentication evidence</summary><pre>{report.authentication.raw_evidence.join('\n\n') || 'No authentication headers observed.'}</pre></details></ReportSection> }