import type { ForensicReport } from '../../types/report'
import { ReportSection } from './ReportSection'
export function EmailMetadataReport({ report }: { report: ForensicReport }) { const metadata = report.email_metadata; const fields = [['From', metadata.sender], ['To', metadata.recipients.join(', ')], ['CC', 'Unavailable'], ['Reply-To', metadata.reply_to.join(', ') || 'Unavailable'], ['Return-Path', metadata.return_path], ['Subject', metadata.subject], ['Date', metadata.date], ['Message-ID', metadata.message_id]]; return <ReportSection eyebrow="Message evidence" title="Email Metadata"><div className="report-field-table">{fields.map(([label, value]) => <div key={label}><span>{label}</span><strong className={label !== 'Subject' && label !== 'Date' ? 'mono' : ''}>{value || 'Unavailable'}</strong></div>)}</div></ReportSection> }