import { Download, Printer } from 'lucide-react'
import { SeverityBadge } from '../ui/SeverityBadge'
import type { ForensicReport } from '../../types/report'

interface ReportHeaderProps { report: ForensicReport; onPrint: () => void; onExport: () => void }
export function ReportHeader({ report, onPrint, onExport }: ReportHeaderProps) { return <header className="report-header"><div><p className="eyebrow">ECHO</p><h1>Forensic Analysis Report</h1><div className="report-identifiers"><span>Report <strong>{report.report_id}</strong></span><span>Incident <strong>{report.incident.incident_id}</strong></span><span>Evidence <strong>{report.evidence_provenance.evidence_id}</strong></span><span>Generated <strong>{report.generated_at}</strong></span></div></div><div className="report-header-right"><div className="report-severity"><SeverityBadge severity={report.incident.severity === 'CRITICAL' ? 'Critical' : report.incident.severity === 'HIGH' ? 'High' : report.incident.severity === 'MEDIUM' ? 'Medium' : 'Low'} /><strong>Risk {report.incident.risk_score}/100</strong><span>{report.incident.threat_type}</span></div><div className="report-actions"><button className="button button-secondary" onClick={onPrint}><Printer size={15} />Print / PDF</button><button className="button button-primary" onClick={onExport}><Download size={15} />Export JSON</button></div></div></header> }